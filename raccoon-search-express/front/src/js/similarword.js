import { _ } from './const';

export default function Similarword() {
  this.string = string;
  this.input = _.$('.search-bar--keyword');
}

Similarword.prototype.addEvent = function () {
  this.input.addEventListener('keyup', requestData);
};
Similarword.prototype.requestData = function () {};
